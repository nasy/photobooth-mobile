import {
    StyleSheet
} from 'react-native';

export const styles = StyleSheet.create({
    content:{
        backgroundColor: 'transparent',
        justifyContent: 'center',
        alignItems: 'center'
    },
    animation: { 
        height: 120, 
        width: 210, 
      },
});